name: depsight
health: /api/health
compose_file: docker-compose.yml
pre_update:
  - docker compose build --no-cache
post_update:
  - docker exec depsight-app-1 npx prisma db push --skip-generate
rollback: true
