name: depsight
health: /api/health
compose_file: docker-compose.traefik.yml
pre_update:
  - docker compose -f docker-compose.traefik.yml build --no-cache
post_update:
  - docker compose -f docker-compose.traefik.yml exec app npx prisma db push --skip-generate
rollback: true
